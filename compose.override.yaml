services:
###> symfony/mercure-bundle ###
  mercure:
    environment:
      # Plain HTTP on a fixed port for local development
      # (dev.Caddyfile is already permissive: cors_origins *, anonymous)
      SERVER_NAME: ':80'
    ports:
      - "3000:80"
###< symfony/mercure-bundle ###
